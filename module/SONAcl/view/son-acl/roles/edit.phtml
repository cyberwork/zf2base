<h3>Novo Role</h3>
<br />
<?php 
$form = $this->form;
$form->setAttribute('role','form');
$form->setAttribute('action',$this->url('sonacl-admin/default',array('controller'=>'roles','action'=>'edit', 'id'=>$form->get("id")->getValue())));
$form->prepare();

echo $this->form()->openTag($form);
foreach ($form->getElements() as $el) :
        ?>
        <div class="form-group">
            <?php echo $this->formRow($el); ?>
        </div>
        <?php
endforeach;
echo $this->form()->closeTag();
?>